{% extends 'base.html.twig' %}

{% block title %}categorie index{% endblock %}
{% block body %}
{<main id="main" class="main">

    <div class="pagetitle">
      <h1>table des categories</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="index.html">Home</a></li>
          <li class="breadcrumb-item">Tables</li>
          <li class="breadcrumb-item active">Data</li>
        </ol>
      </nav>
    </div><!-- End Page Title -->

    <section class="section">
      <div class="row">
        <div class="col-lg-12">

          <div class="card">
            <div class="card-body">
              
              <!-- Table with stripped rows -->
              <table class="table datatable">
                <thead>
                  
            <tr>
                    <td>id </td>
                    <td> nom</td>
                  <td> </td>
                </tr>
                 {% for i in k %}
                 
                <tr>
                    <td>{{ i.id}} </td>
                    <td> {{ i.nom }}</td> 
                    <td>
                 <a class="btn btn-danger"  href="{{path('suprimerCategorie',{id : i.id })}}">Supprimer</a>
                         </td>
                         <td> <a href="{{path('updateCategorie',{'id':i.id  }) }}">update</a></td>
                 
                 
                
                 {% endfor %}
                 </tbody>
              </table>
              {{ knp_pagination_render(k) }}
              <!-- End Table with stripped rows -->

            </div>
          </div>

        </div>
      </div>
    </section>

  </main>

{% endblock %}
  